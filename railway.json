{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf node_modules/.cache 2>/dev/null || true && npm install --legacy-peer-deps && npm run build",
    "startCommand": "npm run preview"
  }
}
